package dev.mikoto2000.rei.conversation;

/** Retrieval scope only; never changes conversation persistence. */
public enum HistorySearchScope {
  CURRENT_PROJECT_ONLY, CURRENT_PROJECT_PREFERRED, ALL_PROJECTS
}
